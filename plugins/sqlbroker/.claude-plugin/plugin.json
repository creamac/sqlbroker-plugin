{
  "name": "sqlbroker",
  "version": "2.0.0",
  "description": "Alias-based MSSQL broker for Claude Code on Windows. A local broker holds passwords in the OS keyring (Windows Credential Manager) by alias; the conversation never carries passwords. v2 auto-migrates legacy DPAPI aliases.",
  "author": {
    "name": "pumipat"
  },
  "homepage": "https://github.com/creamac/sqlbroker-plugin",
  "license": "MIT",
  "keywords": [
    "mssql",
    "sql-server",
    "windows",
    "mcp",
    "broker",
    "keyring"
  ],
  "platforms": ["win32"],
  "mcpServers": {
    "sqlbroker": {
      "command": "${CLAUDE_PLUGIN_ROOT}/scripts/run_stdio_proxy.bat",
      "args": []
    }
  }
}
