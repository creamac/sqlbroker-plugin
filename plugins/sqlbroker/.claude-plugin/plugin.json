{
  "name": "sqlbroker",
  "version": "2.1.0",
  "description": "Alias-based MSSQL broker for Claude Code. Cross-platform: Windows uses Task Scheduler + Credential Manager; Linux uses systemd + Secret Service; macOS uses launchd + Keychain. Passwords live in the OS keyring; the conversation never carries them. Auto-migrates v1 DPAPI aliases.",
  "author": {
    "name": "pumipat"
  },
  "homepage": "https://github.com/creamac/sqlbroker-plugin",
  "license": "MIT",
  "keywords": [
    "mssql",
    "sql-server",
    "windows",
    "mcp",
    "broker",
    "keyring"
  ],
  "platforms": ["win32"],
  "mcpServers": {
    "sqlbroker": {
      "command": "${CLAUDE_PLUGIN_ROOT}/scripts/run_stdio_proxy.bat",
      "args": []
    }
  }
}
